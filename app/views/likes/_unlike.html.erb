<%= button_to metaphor_like_path(user_id: current_user.id, metaphor_id: metaphor.id), method: :delete, class: "btn btn-success btn-xs", remote: true do %>
    Likeを取り消す
<% end %>
<span><%= metaphor.likes_count %></span>